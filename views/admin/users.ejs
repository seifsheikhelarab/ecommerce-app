<%-  include('../partials/adminHeader.ejs')  %>

<div class="container mt-5">
    <h2>All Users</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>User ID</th>
                <th>Username</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach(user => { %>
            <tr>
                <td><%= user._id %></td>
                <td><%= user.username %></td>
                <td><%= user.email %></td>
                <td>
                    <a href="/admin/user/edit/<%= user._id %>" class="btn btn-warning btn-sm">Edit</a>
                    </form>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>
</div>


<%- include('../partials/footer.ejs') %>
